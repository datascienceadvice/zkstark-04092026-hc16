# zkstark: ZK-протокол выбора между Уэлча t-тестом и медианным тестом Муда

Черновик статьи. Подробности структуры и запуска — в `README.md`; руководство
для агентов (включая механику композиции) — в `AGENTS.md`.

## Аннотация

We build a two-party zero-knowledge comparator for two independent samples on
the Risc Zero zkVM. The protocol automatically selects, based on the data, a
Welch t-test or Mood's median test with an exact Fisher two-sided p-value, so
laboratories publish only aggregate statistics and never raw observations.
Correctness of the whole comparison is a single, composable proof: on each of
the two protocol rounds the aggregator verifies both laboratory receipts
*inside the guest* via `env::verify`, so the final aggregator receipt is valid
only if all six laboratory proofs are valid and their journals match the input.

## Протокол (кратко)

Двухраундовая схема «лаборатория → агрегатор → лаборатория → агрегатор»,
6 доказательств на одно сравнение:

- **Раунд 1** — каждая лаборатория доказывает сводные статистики
  (n, sum, sum²), критерий Шапиро–Уилка (порт R `shapiro.test`,
  `swilk.c` + `shapiro.test.R`) и медиану группы. Агрегатор выбирает метод:
  обе группы нормальны → Уэлча t-тест; иначе → медианный тест Муда с
  M̂ = mean(median₁, median₂).
- **Раунд 2** — лаборатории повторно материализуют статистики и, только при
  Мood, считают `count_above(M̂)` по группам (a, b). Агрегатор формирует
  2×2-таблицу и вычисляет точный двусторонний р-уровень Фишера.
- **Композиция**: на обеих стадиях агрегатор проверяет лабораторные
  доказательства внутри гостя
  `env::verify(LAB_ID_DIGEST, &lab_journal)`; итоговый receipt верифицируется
  бенчмарком извне по method_id.

Весь численный слой реализован вручную в `core/src/*`, без внешних
стат-крейтов: спецфункции и распределения (Lanczos `ln_gamma`, неполная
бета NR, pnorm/qnorm по AS 66 / AS 241, swilk) — порты R и совпадают с
R 4.3.1 до ~1e-12.

## Приватность

Наружу (в journal) уходят только агрегаты:

- Уэлча-путь: (n, sum, sum²) каждой группы — эквивалент mean/sd.
- Mood-путь: дополнительно медианы групп, M̂ и `count_above(M̂)`. Суммарный
  ответ на порог M̂ — осознанный компромисс, необходимый для 2×2-таблицы.

## Композиция доказательств (техническая деталь)

Risc Zero: гостевой receipt’а лаборатории имеет контракт
`ReceiptClaim::ok(LAB_ID, journal)` (`pre: Pruned(image_id)`,
`post: {pc:0, merkle_root:0}`, `exit Halted(0)`, `input: None`, пустые
assumptions), поскольку вход гостя вводится через `env::read` без явного
`input_digest` (по умолчанию ZERO). Это делает лабораторные receipts
пригодными для `env::verify`. Раннер передаёт их агрегатору через
`ExecutorEnvBuilder::add_assumption`; агрегатор сверяет journal-байты
(`lab1_journal`/`lab2_journal` во входе) с теми, что подписаны доказательством.

`core::LAB_ID_DIGEST` держится синхронно с гостевой сборкой
(`methods::LAB_ID` из `target/release/build/methods-*/out/methods.rs`) —
image_id стабилен между пересборками, пока код гостя не менялся.

Гости собираются на **гостевом std** (`features=["std"]`): для таргета
`riscv32im-risc0-zkvm-elf` f64-математика (`sqrt`, `powf`, …) доступна только
в std, поэтому строгий `#![no_std]`-гость без libm-замен невозможен.
Математика `core` при этом остаётся no_std-совместимой.

## R2-оптимизация

При выборе Уэлча медиана группы в раунде 2 не нужна (метод не использует
M̂), поэтому гость пропускает сортировку медианы
(`materialize(sample, need_median)` при `need_median=false` кладёт
`median = f64::NAN`, у Уэлча `Stage1Decision.m_hat = None`).

## Результаты

20 фиксированных сценариев (`results/scenarios.csv`), golden из R 4.3.1
(`results/golden.csv`); каждая метрика journal сверяется с golden в допуске
`--tol 1e-9`; наблюдаемые погрешности ≤ ~2e-12. Таблицы ниже перегенерируются
скриптами `analysis/make_paper_tables.R` / `analysis/make_tables.R` из экспорта
`results/report_*.csv` — в тексте встроены актуальные выкладки
(`analysis/tables/table_paper_*.md`).

### Dev-матрица (fake-receipts, честный journal)

Прогон `results/report_16.csv`: 20/20 сценариев, 19 `ok` + 1 `expected_error`
(константная группа n15 — намеренная SW-ошибка), все сверки `pass`. Метод
выбирается протоколом по Шапиро–Уилку (α = 0.05): обе группы нормальны → Уэлча,
иначе → Муд. Суммарно ~86.6M циклов за ~9 с.

| Сценарий | Описание | n₁/n₂ | Метод | SW p₁ | SW p₂ | p (критерий) | макс. отн. ошибка | pass | время, с | циклы |
|---|---|---|---|---|---|---|---|---|---|---|
| n01 | Нормальные, равные (30/25) | 30/25 | Уэлча | 0.544 | 0.441 | 0.240 | 8.4e-14 | да | 0.40 | 1 441 792 |
| n02 | Нормальные, смещение среднего (8/8) | 8/8 | Уэлча | 0.112 | 0.387 | 0.765 | 1.5e-14 | да | 0.31 | 917 504 |
| n03 | Нормальные, неравные дисперсии (60/60) | 60/60 | Уэлча | 0.584 | 0.105 | 0.014 | 5.3e-14 | да | 0.38 | 2 490 368 |
| n04 | Нормальные, неравные n (200/30) | 200/30 | Уэлча | 0.523 | 0.229 | 0.300 | 2.6e-13 | да | 0.42 | 3 407 872 |
| n05 | Нормальные, n = 3/4 | 3/4 | Уэлча | 0.350 | 0.276 | 0.241 | 1.2e-14 | да | 0.31 | 786 432 |
| n06 | Логнормальные (40/40) | 40/40 | Муд | 0.245 | 0.017 | 4.9e-08 | 6.1e-14 | да | 0.46 | 4 849 664 |
| n07 | Гамма, shape = 2 (40/40) | 40/40 | Муд | 0.000 | 0.001 | 0.261 | 6.9e-14 | да | 0.45 | 5 373 952 |
| n08 | Экспоненциальные (50/50) | 50/50 | Муд | 2.0e-05 | 3.3e-06 | 0.069 | 4.8e-14 | да | 0.52 | 5 636 096 |
| n09 | Равномерные (35/35) | 35/35 | Муд | 0.022 | 0.018 | 0.008 | 7.1e-14 | да | 0.47 | 4 325 376 |
| n10 | t₃, тяжёлый хвост (45/45) | 45/45 | Муд | 0.024 | 0.010 | 0.003 | 9.2e-14 | да | 0.50 | 5 636 096 |
| n11 | Бимодальная смесь (40/40) | 40/40 | Муд | 0.000 | 5.2e-05 | 1.000 | 5.1e-14 | да | 0.46 | 5 373 952 |
| n12 | Дискретные 1:5 (50/50) | 50/50 | Муд | 8.3e-07 | 0.000 | 0.838 | 8.3e-15 | да | 0.50 | 5 373 952 |
| n13 | Крупные нормальные (1000/1000) | 1000/1000 | Уэлча | 0.511 | 0.835 | 4.0e-13 | 1.8e-12 | да | 1.36 | 24 772 608 |
| n14 | Граница n = 11/12 | 11/12 | Уэлча | 0.997 | 0.281 | 0.100 | 1.2e-14 | да | 0.31 | 917 504 |
| n15 | Константная группа (20/20) | 20/20 | — (SW), ошибка | — | — | — | 0 | да | 0.00 | 0 |
| n16 | t₂, n = 3/3 | 3/3 | Уэлча | 0.839 | 0.417 | 0.345 | 5.6e-15 | да | 0.29 | 655 360 |
| n17 | Полунормальные (38/38) | 38/38 | Муд | 0.002 | 0.001 | 0.357 | 6.3e-14 | да | 0.45 | 5 373 952 |
| n18 | Пуассоновские счёта (60/60) | 60/60 | Муд | 0.004 | 0.026 | 1.9e-05 | 1.8e-13 | да | 0.54 | 6 422 528 |
| n19 | Равные средние, разные sd (25/25) | 25/25 | Уэлча | 0.860 | 0.155 | 0.396 | 1.8e-14 | да | 0.33 | 1 441 792 |
| n20 | Разность средних (32/32) | 32/32 | Уэлча | 0.556 | 0.679 | 1.2e-05 | 3.1e-14 | да | 0.33 | 1 441 792 |

### Full-STARK (настоящие доказательства)

Прогон `results/report_17.csv`: n01, n02, n16 (Уэлча, включая n = 3) — все
`pass`; композиция через `env::verify` подтверждена в боевом режиме. Медианный
сценарий n06 в full упирается в память prover-сервера (`prove: rx len failed` —
падение r0vm-процесса на 11 сегментах, не логика); корректность Mood
подтверждается dev-матрицей выше.

| Сценарий | Описание | n₁/n₂ | Метод | макс. отн. ошибка | время, с | циклы | сегментов |
|---|---|---|---|---|---|---|---|
| n01 | Нормальные, равные (30/25) | 30/25 | Уэлча | 8.4e-14 | 817 | 1 441 792 | 6 |
| n02 | Нормальные, смещение среднего (8/8) | 8/8 | Уэлча | 1.5e-14 | 515 | 917 504 | 6 |
| n16 | t₂, n = 3/3 | 3/3 | Уэлча | 5.6e-15 | 368 | 655 360 | 6 |

## Оценка точности медианного теста (аппроксимация M̂)

2×2-таблица для теста Муда строится относительно порога M̂ = mean(median₁,
median₂) — аппроксимации истинной объединённой медианы, необходимой, чтобы
лаборатории не публиковали ранжирование всей объединённой выборки. Ниже —
сравнение нашего p (по M̂) с «эталоном»: точным двусторонним p Фишера для
таблицы, построенной по истинной объединённой медиане `median(c(x, y))`
(считается вне протокола, в R). Погрешность зависит от сценария и **не исчезает
с ростом n**: max |exact − approx| = 0.571. При этом аппроксимация **не
искажает вывод**: ложных значимостей нет — все 7 сценариев со значимым p по M̂
(n03, n06, n09, n10, n13, n18, n20) остаются значимыми и по точной
объединённой медиане. Единственный разлёт решений — пограничный n01
(p_M̂ = 0.060 против p_exact = 0.031, нормальные, n = 30/25), где
аппроксимация не достигает α = 0.05; этот сценарий протокол выводит на
Уэлча-ветвь (по Шапиро–Уилку обе группы нормальны), так что на фактический
результат аппроксимация не влияет.

| Сценарий | Описание | M̂ | a | b | p (наш, M̂) | p (истинная медиана) | разность p |
|---|---|---|---|---|---|---|---|
| n01 | Нормальные, равные (30/25) | 99.76 | 19 | 9 | 0.060 | 0.031 | -0.02918 |
| n02 | Нормальные, смещение среднего (8/8) | 0.1366 | 3 | 6 | 0.315 | 0.619 | 0.3046 |
| n03 | Нормальные, неравные дисперсии (60/60) | 54.03 | 9 | 35 | 1.3e-06 | 0.000 | 4.7e-04 |
| n04 | Нормальные, неравные n (200/30) | 1.046 | 89 | 18 | 0.121 | 0.170 | 0.04873 |
| n05 | Нормальные, n = 3/4 | 0.8926 | 0 | 2 | 0.429 | 1.000 | 0.5714 |
| n06 | Логнормальные (40/40) | 1.498 | 4 | 28 | 4.9e-08 | 1.2e-07 | 6.8e-08 |
| n07 | Гамма, shape = 2 (40/40) | 1.107 | 25 | 19 | 0.261 | 0.823 | 0.5622 |
| n08 | Экспоненциальные (50/50) | 2 | 17 | 27 | 0.069 | 0.317 | 0.2481 |
| n09 | Равномерные (35/35) | 0.7868 | 11 | 23 | 0.008 | 0.004 | -0.004256 |
| n10 | t₃, тяжёлый хвост (45/45) | 0.4933 | 16 | 31 | 0.003 | 0.001 | -0.002261 |
| n11 | Бимодальная смесь (40/40) | 0.71 | 20 | 20 | 1.000 | 1.000 | 0 |
| n12 | Дискретные 1:5 (50/50) | 3 | 21 | 19 | 0.838 | 0.838 | 0 |
| n13 | Крупные нормальные (1000/1000) | 10.28 | 422 | 561 | 6.3e-10 | 8.5e-10 | 2.1e-10 |
| n14 | Граница n = 11/12 | 0.4964 | 4 | 8 | 0.220 | 0.414 | 0.1933 |
| n16 | t₂, n = 3/3 | 0.7112 | 1 | 2 | 1.000 | 1.000 | 0 |
| n17 | Полунормальные (38/38) | 0.7786 | 15 | 20 | 0.357 | 0.492 | 0.1342 |
| n18 | Пуассоновские счёта (60/60) | 4.5 | 21 | 45 | 1.9e-05 | 2.5e-05 | 6.1e-06 |
| n19 | Равные средние, разные sd (25/25) | -0.1325 | 15 | 12 | 0.571 | 1.000 | 0.4291 |
| n20 | Разность средних (32/32) | 0.4898 | 10 | 24 | 0.001 | 0.000 | -8.3e-04 |

## Ограничения / открытые вопросы

- Аппроксимация медианного теста M̂ (см. раздел «Оценка точности…»): max
  |exact − approx| = 0.571 (n05); заметна и на умеренных n (n07: 0.562,
  n19: 0.429, n02: 0.305); исчезает лишь при малых p-значениях/больших
  эффектах (n06: ~7e-8, n18: ~6e-6). Вывод протокола при этом устойчив:
  0 ложных значимостей на 19 сценариях (7/7 значимых по M̂ остаются значимыми
  по точной медиане); единственный разлёт — n01, закрываемый Уэлча-ветвью.
  Методологическая альтернатива — тест Муда с χ²-приближением вместо точного
  p Фишера; здесь выбран вариант с точным двусторонним p.
- Производительность полного proof для крупных кейсов (n=1000, ~25M циклов)
  высока; в CI — dev-режим.

## Воспроизводимость с нуля

Среда: Windows (исходники в `D:\lair\zkstark`, R 4.3.1) + WSL2/Ubuntu
(реальный `~/zkstark`; после правок на Windows — `bash scripts/sync_to_wsl.sh`).
Нативная сборка на Windows невозможна: гостям нужен тулчейн
`riscv32im-risc0-zkvm-elf` (rizk0, rzup). Версии: risc0-zkvm 2.3.2, R 4.3.1.
Не меняйте раздел сценариев в `scripts/golden.R` без перегенерации golden
и сверки с R — golden служит эталоном.

```bash
# --- все команды в WSL, cwd ~/zkstark -------------------------------
cargo test -p zkstark-core                                   # ядро: 25 тестов

# демо полного протокола (dev, 6 proof на сравнение, zk == CPU)
cargo run -p host --release

# dev-матрица, вся матрица (>10 c): fake-receipts, честный journal
cargo run -p bench --release --bin bench -- --mode dev --export
#    → results/report_<N>.csv (последний прогон с 20 кейсами = dev-матрица)

# настоящие STARK-доказательства, по одному сценарию (~6–14 мин/кейс)
cargo run -p bench --release --bin bench -- --mode full --cases n01,n02,n16 --export

# --- Windows, R ------------------------------------------------------
Rscript analysis/make_paper_tables.R   # analysis/tables/table_paper_{dev,full,mood}.md
Rscript analysis/make_tables.R         # table_*.csv + лог make_tables_out.txt (UTF-8)
```

Скрипты R выбирают актуальные прогоны автоматически: dev = самый свежий
экспорт с 20 кейсами, full = самый свежий другой экспорт. Результаты
(база `results/zkstark.db`, отчёты, golden, таблицы) — фиксированные
артефакты в репозитории.