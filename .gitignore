# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Rust
/target
**/target

# SQLite — транзиентные файлы journaling (сама БД zkstark.db коммитится)
results/*.db-wal
results/*.db-shm

# Логи экспериментов и прогонов (артефакты уже в results/*.csv)
scripts/*.txt
analysis/*.txt
# Временные файлы Word (lock-файлы)
~$*
