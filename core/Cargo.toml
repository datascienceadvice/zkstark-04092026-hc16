[package]
name = "zkstark-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
# Статистическое ядро — типы сериализуются через serde для ввода/журнала zkVM.
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]